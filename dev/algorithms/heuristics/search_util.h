#ifndef OPTIQ_SEARCH_UTIL
#define OPTIQ_SEARCH_UTIL

#include <vector>

bool check_and_reverse(std::vector<std::pair<int, std::vector<int> > > &all_sd, std::vector<std::pair<int, std::vector<int> > > &sd, int num_nodes);

#endif
